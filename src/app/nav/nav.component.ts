import { Component } from '@angular/core';

@Component({
  selector: 'app-nav',
  templateUrl: './nav.component.html', // External HTML
  styleUrls: ['./nav.component.css'], // External CSS (optional)
})
export class NavComponent {
  
}
